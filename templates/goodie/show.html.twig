{% extends 'base.html.twig' %}

{% block title %}Goodie{% endblock %}

{% block body %}
    <h1>Goodie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ goodie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ goodie.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_goodie_index') }}">back to list</a>

    <a href="{{ path('app_goodie_edit', {'id': goodie.id}) }}">edit</a>

    {{ include('goodie/_delete_form.html.twig') }}
{% endblock %}
